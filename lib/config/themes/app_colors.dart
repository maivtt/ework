import 'package:flutter/widgets.dart';

/// Application-wide constants. Split into different areas: theme, layout and
class AppColors {
  /// Primary color of the app.
  static const Color primaryColor = Color(0xffffdb00);
}
